# Auth Migration Context

This example exists to demonstrate Ledge operability.

It intentionally contains a mismatch:

- the knowledge claim says authentication uses BetterAuth,
- the source reality still references Clerk.

The mismatch is the drift this reference implementation is expected to detect.
